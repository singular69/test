<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>เข้าสู่เกมนักสืบ</title>
  <link rel="stylesheet" href="style.css">
</head>
<body>
  <div id="game-container">
    <h1>ยินดีต้อนรับเข้าสู่เกมนักสืบ</h1>
    <p>กรุณาใส่ชื่อของคุณ:</p>
    <input type="text" id="player-name" placeholder="ชื่อของคุณ">
    <button id="start-game">เริ่มเกม</button>
  </div>

  <script>
    document.getElementById('start-game').addEventListener('click', () => {
      const playerName = document.getElementById('player-name').value.trim();
      if (playerName) {
        sessionStorage.setItem('playerName', playerName); // บันทึกชื่อใน sessionStorage
        window.location.href = 'character.html'; // ไปที่หน้าเลือกตัวละคร
      } else {
        alert('กรุณาใส่ชื่อของคุณ!');
      }
    });
  </script>
</body>
</html>